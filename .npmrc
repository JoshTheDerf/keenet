; Supply-chain posture for a password manager:
; - save-exact: new deps are pinned to exact versions (no ^/~ ranges);
;   transitives are pinned by package-lock.json.
; - audit on install so known-vulnerable versions surface immediately.
save-exact=true
audit=true
fund=false
